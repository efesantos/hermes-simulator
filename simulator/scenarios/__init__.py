"""Scenario content: Stage-1 pre-filter tasks and multi-day personas.

The data *contracts* live in :mod:`simulator.scenarios.types` (defined with U3,
the first consumer). Concrete content and validation are authored on top of them:
Stage-1 tasks in ``stage1/`` (U4), the persona schema/loader in ``personas/``
(U5).
"""
